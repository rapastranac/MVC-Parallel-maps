name: C/C++ Windows

on:
  push:
    branches: 
      - master

jobs:
  build:

    runs-on: windows-latest
   
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: ./a.out
    - name: Install Dependencies
      run: npm install
